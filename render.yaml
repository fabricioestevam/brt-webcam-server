services:
  - type: web
    name: brt-webcam-server
    env: python
    plan: free
    branch: main
    region: oregon
    buildCommand: "pip install --upgrade pip && pip install -r requirements.txt"
    startCommand: "gunicorn server:app --bind 0.0.0.0:$PORT"
    pythonVersion: "3.11.9"
